import sys

def solve(N):
    temp = list(range(N))
    print(' '.join([1, 2, 3]))
    res = int(sys.stdin.readline())

    

line = sys.stdin.readline().rsplit('\n')
T, N, Q = [int(x) for x in line.split(' ')]
for _ in range(T):
    print()
